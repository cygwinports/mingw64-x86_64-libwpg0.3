--- origsrc/libwpg-0.1.3/src/lib/Makefile.am	2007-09-03 16:38:39.000000000 -0500
+++ src/libwpg-0.1.3/src/lib/Makefile.am	2009-04-27 14:40:33.373375000 -0500
@@ -1,8 +1,12 @@
 if PLATFORM_WIN32
 no_undefined = -no-undefined
-version_info = -version-info $(LT_CURRENT):$(LT_REVISION):$(LT_AGE) -avoid-version
 else
 no_undefined =
+endif
+
+if OS_WIN32
+version_info = -version-info $(LT_CURRENT):$(LT_REVISION):$(LT_AGE) -avoid-version
+else
 version_info = -version-info $(LT_CURRENT):$(LT_REVISION):$(LT_AGE)
 endif
 
